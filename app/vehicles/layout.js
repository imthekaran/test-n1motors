// ISR Configuration: Incremental Static Regeneration
// This page will be statically generated and revalidated every 300 seconds (5 minutes)
// After the revalidate period, Next.js will serve the cached page to visitors
// On the next request after revalidation, the page is regenerated in the background
export const revalidate = 300;

export default function VehiclesLayout({ children }) {
  return children;
}
